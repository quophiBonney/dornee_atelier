# Task: Dashboard Overview — Real DB Values + 3 Cards

## Steps

- [x] 1. Update inline `KpiCard` in `AdminDashboard.jsx` to support `loading` state and drop static delta props.
- [x] 2. Rewrite inline `OverviewPage` to fetch real users/appointments/contacts and render 3 cards (Users, Appointments, Enquiries).
- [x] 3. Remove "Response rate" card and switch grid to `xl:grid-cols-3`.
- [x] 4. Update sidebar `counts` in `Dashboard` to use real Redux data.
- [x] 5. Verify build / dev server.
